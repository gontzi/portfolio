---
// CTA.astro
---

<section class="cta-section">
  <div class="container">
    <div class="cta-box fade-in">
      <div class="cta-content">
        <h3 class="cta-title">
          ¿Tu próximo proyecto web podría ser así de impactante?
        </h3>
        <p class="cta-description">
          He ayudado a emprendedores y empresas a convertir sus ideas en webs
          que realmente funcionan y venden. Tu proyecto merece la misma
          dedicación personalizada.
        </p>

        <div class="cta-calendar-container">
          <div id="cta-google-calendar-button"></div>
        </div>

        <p class="cta-guarantee">
          <span class="guarantee-item">Agenda fácil en 1 minuto</span>
          <span class="guarantee-separator">•</span>
          <span class="guarantee-item">Respondo en menos de 24h</span>
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .cta-section {
    padding: 8rem 0;
    background-color: var(--bg-color);
  }

  .cta-box {
    background-color: var(--text-main);
    color: var(--bg-color);
    padding: 4rem 2rem;
    border-radius: 8px;
    box-shadow: 12px 12px 0px var(--accent);
    position: relative;
    overflow: hidden;
  }

  /* Retro decorative dots */
  .cta-box::before {
    content: "";
    position: absolute;
    top: 0;
    right: 0;
    width: 150px;
    height: 150px;
    background-image: radial-gradient(var(--accent) 2px, transparent 2px);
    background-size: 15px 15px;
    opacity: 0.2;
  }

  .cta-content {
    text-align: center;
    max-width: 700px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }

  .cta-title {
    font-size: 2.5em;
    font-family: var(--font-heading);
    color: var(--bg-color);
    margin-bottom: 1.5rem;
    line-height: 1.2;
  }

  .cta-description {
    font-size: 1.25em;
    color: var(--bg-color);
    opacity: 0.9;
    margin-bottom: 3rem;
    line-height: 1.6;
    font-family: var(--font-body);
  }

  .cta-calendar-container {
    margin-bottom: 2.5rem;
    display: flex;
    justify-content: center;
  }

  .cta-guarantee {
    opacity: 0.7;
    font-size: 0.9em;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    font-family: var(--font-body);
  }

  .guarantee-separator {
    color: var(--accent);
  }

  @media (max-width: 768px) {
    .cta-section {
      padding: 4rem 1rem;
    }
    .cta-box {
      padding: 3rem 1.5rem;
      border-radius: 12px;
      box-shadow: 8px 8px 0px var(--accent);
    }

    .cta-title {
      font-size: 1.8em;
      margin-bottom: 1rem;
    }

    .cta-description {
      font-size: 1.05em;
      margin-bottom: 2rem;
    }

    .cta-guarantee {
      flex-direction: column;
      gap: 0.5rem;
      font-size: 0.8em;
    }

    .guarantee-separator {
      display: none;
    }

    .cta-calendar-container {
      margin-bottom: 1.5rem;
    }
  }
</style>

<script>
  (function () {
    window.addEventListener("load", function () {
      // @ts-ignore
      calendar.schedulingButton.load({
        url: "https://calendar.google.com/calendar/appointments/schedules/AcZssZ1QF1BIcez4Jt9OWjC8hrZd7a34UfemQncXGpb1P-g7RRPvegXfnJFlihzR-x_ujcx68apg_LlY?gv=true",
        color: "#E3A83F", // Mustard for CTA
        label: "Hablemos de tu proyecto",
        target: document.getElementById("cta-google-calendar-button"),
      });

      setTimeout(() => {
        const container = document.getElementById("cta-google-calendar-button");
        if (container && container.nextElementSibling) {
          const button = container.nextElementSibling as HTMLElement;

          button.style.backgroundColor = "#E3A83F"; // Mustard
          button.style.color = "#1A2C38"; // Navy
          button.style.border = "2px solid #E3A83F";
          button.style.padding = "18px 40px";
          button.style.fontSize = "1em";
          button.style.textTransform = "uppercase";
          button.style.fontWeight = "800";
          button.style.borderRadius = "4px";
          button.style.boxShadow = "6px 6px 0px #F3F0E7"; // Cream shadow
          button.style.transition = "all 0.2s ease";

          button.addEventListener("mouseenter", () => {
            button.style.transform = "translate(2px, 2px)";
            button.style.boxShadow = "3px 3px 0px #F3F0E7";
          });

          button.addEventListener("mouseleave", () => {
            button.style.transform = "translate(0, 0)";
            button.style.boxShadow = "6px 6px 0px #F3F0E7";
          });
        }
      }, 500);
    });
  })();
</script>
